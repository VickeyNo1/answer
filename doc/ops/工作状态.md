# 工作状态交接（更新于 2026-07-29）

> 用途：跨设备/跨会话继续开发时，让 AI 助手和开发者快速恢复上下文。每次阶段性收尾时更新本文件。

## 当前状态：v4.0 M1（试点底座）开发完成 ✅ 待发版上线 ｜ M2（考试系统）未开工 📋

- 生产地址：http://8.148.219.179 （机器B，Nginx → Next.js:3000 / FastAPI:8000）
  - 注意：7 月下旬本地调试期间曾 `systemctl stop answer-frontend answer-backend`，上线前确认已 `start` 恢复
- 全链路已验证：登录 → 对话 → 大模型（百炼）→ 知识库检索（机器A :8100，带 X-KB-Token）→ 知识点标注
- 数据库：机器A MySQL 8.4（私网 172.22.207.228:3306，库 answer，**8 张表**）；M1 新增 `feedbacks` / `kb_search_logs` / `app_settings` 三表 + users 幂等 ALTER 补 `daily_question_limit`、`memory_enabled` 两列（NULL=跟随全局），启动 `init_db()` 自动建
- **v4.0 设计文档为终稿（含评审修订）**：`doc/plans/2026-07-29-v4.0-个性化学习版-设计.md`（三里程碑：M1 试点底座 / M2 考试系统 / M3 学生记忆），M1 已按其全量交付
- 日常发版流程：本地 push → 机器B 执行 `bash /opt/answer/deploy/deploy.sh`（5 步：pull → uv sync+seed → npm ci+build → 重启 → 健康检查；健康检查已改为仅 `status=ok` 判成功）
- `.env` 本次无变更，无需同步

### v4.0 M1 交付摘要（2026-07-29）

- 后端：8 个新接口——`POST /api/feedback`、`GET /api/admin/feedbacks`、`GET /api/admin/kb/stats`、`GET /api/admin/kb/hot-kps`、`GET/PUT /api/admin/settings`、`PUT /api/admin/students/{id}/entitlements`、`PUT /api/me/password`；`/api/health` 升级 `status/mysql/kb` 三字段真实探测；聊天链路加每日配额 429、并发闸门+有限排队（`app/chat/concurrency.py`，threading.BoundedSemaphore，勿改 asyncio）、SSE 新事件 `queue/kb_refs/suggestions`、检索日志六态落库、追问建议 `===SUGGESTIONS===` holdback 解析；新模块 `app/settings_store.py`、`app/admin/entitlements.py`
- 前端：反馈点赞/点踩、引用卡片、追问建议、排队提示、改密码弹窗（`ChangePasswordModal`）、运营报表 Tab（`OperationsTab`）、学生权益编辑、移动端 <768px 抽屉布局
- 文档：`doc/接口文档.md` 升 v4.0（28 接口）、AGENTS.md 同步（8 张表/28 接口）
- 验证：pytest 147 passed / 62 skipped（e2e 存量跳过）；tsc 零错误；三维度代码评审问题全部修复；两轮浏览器 E2E 通过，期间修复真实 bug——`GET /api/admin/students` 的 SELECT 漏权益两列导致前端永远显示「跟随全局」

## 新电脑上手步骤

1. `git clone git@github.com:VickeyNo1/answer.git`
2. 创建 `backend/.env`（**含密钥不入库**，从旧电脑安全拷贝，或按 [部署文档](部署文档.md) 模板重填）
3. 后端：`cd backend && uv sync`；前端：`cd frontend && npm install`
4. 本地开发注意：本地能否直连机器A MySQL 取决于宽带出口 IP 是否固定——**主力 Windows 机出口 IP 已加安全组+MySQL 用户白名单可直连**（真实出口 IP 以 MySQL 1130 报错为准，网页测的 IP 可能是代理不可信）；**CGNAT 宽带（出口 IP 漂移）的电脑不可直连**，需走 SSH 隧道或在机器B 上调试

## 待办事项（按优先级）

- [ ] **M1 发版上线**：机器B 执行 `bash /opt/answer/deploy/deploy.sh`（seed.py 幂等写入默认全局设置）
- [ ] 安全收尾三件套（运维操作，随 M1 上线一起做）：删机器A 安全组临时规则（3306 公网白名单、22 的 0.0.0.0/0）；修改 admin 初始密码（admin123）；知识库 X-KB-Token 换随机长串（机器A `/opt/cpa/kb.env` 与机器B `backend/.env` 两侧同步改 + 重启）
- [ ] 移动端 <768px 抽屉布局：代码已实现但未经真实视口验证（自动化环境无法缩窗），需人工用 DevTools 设备模拟过一遍
- [ ] **M2（考试系统）开工**：实施计划已制定（`doc/plans/2026-07-29-v4.0-M2-考试系统-实施计划.md`，B1→B4 四批次）；知识库侧（用户自维护）需在 B4 联调前实现 `POST /kb/exam/draw`（契约见 v4.0 设计文档 §4.1），B1-B3 后端用 mock 不阻塞
- [ ] 改密码「旧密码错误」界面提示不够醒目（功能正确，UX 观察项）
- [ ] 分页参数命名不统一：students 用 `size`、feedbacks 用 `page_size`（后续统一）
- [ ] 技术债：存量代码 7 个 lint 错误（非 M1 引入）；存量 chat/admin 路由 `get_db_ctx` 用法为 P1 债（M1 未重构，新路由已全部用 `Depends(get_db)`）

## 新踩的坑

- 浏览器自动化对 React 表单直接 DOM 赋值不触发 onChange，会造成「登录失败」假象，须用真实键盘输入

## 环境备忘（详见 [部署文档](部署文档.md) 与 AGENTS.md）

- 机器A（数据）：Alibaba Cloud Linux 3，公网 8.134.97.196 / 私网 172.22.207.228，MySQL + 知识库服务（kb-server.service，/opt/cpa）
- 机器B（应用）：Alibaba Cloud Linux 2（glibc 2.17、git 1.8.3），公网 8.148.219.179，/opt/answer
  - Node 20 为 unofficial glibc-217 构建；npm registry 用 npmmirror；uv 在 `~/.local/bin`
  - **package-lock.json 以机器B 的 npm 10.8.2 为准**（本地 Windows npm 重生成 lock 会触发 Invalid Version bug，勿在本地重生成）
- 百炼额度：免费额度已耗尽时需在控制台关闭"仅使用免费额度"或切换有额度的模型
